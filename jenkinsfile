pipeline {
    agent any

    stages {

        stage ('build') {
            steps {
                sh 'mvn clean compile -B'
            }
        }

        stage ('test') {
            steps {
                sh 'mvn test -B'
            }
        }
    }
}